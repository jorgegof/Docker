http://kb.asipto.com/asterisk:realtime:kamailio-4.0.x-asterisk-11.3.0-astdb.

jorgegof

rconIFPB2019.1

stop all containers

docker stop $(docker ps -a -q)


remove all containers

docker rm $(docker ps -a -q)


remove all images

docker rmi -f $(docker images -a -q)


kamctl ul show

kamctl db show subscriber

kamctl online


Install PING

apt-get install iputils-ping


Install NETSTAT

apt install net-tools

tcpdump

ngrep sngrep

docker inspect

cat /etc/*-release"

apt-cache search mysql | grep mysql-server

SET PASSWORD FOR 'user-name'@'localhost' = PASSWORD('NEW_USER_PASSWORD');FLUSH PRIVILEGES;
mysql -u user-name -h localhost -p

SHOW DATABASES;
DROP DATABASE name;

INSTALEI O KAMAILIO NO HOST? 5060:5060/udp 5060:5060/tcp

docker run -it --net=host "imagem"

apt-get install default-libmysqlclient-dev

jorge.filho@academico.ifpb.edu.br	

Dw79825
